import { DataOperationService } from './data-operation-service';

describe('DataOperationService', () => {
  it('should create an instance', () => {
    expect(new DataOperationService()).toBeTruthy();
  });
});
